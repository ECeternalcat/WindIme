<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="GarahoTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:buttonStyle">@android:style/Widget.Button</item>
    </style>

    <style name="GarahoLauncherTheme" parent="android:style/Theme.Translucent.NoTitleBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>

    <!-- Transparent host for first-use prompts (e.g. keymap setup). An Activity
         owns a normal window token, so it needs no SYSTEM_ALERT_WINDOW
         permission (unlike a dialog shown directly from the IME Service). -->
    <style name="GarahoPromptTheme" parent="android:style/Theme.Translucent.NoTitleBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>
</resources>
